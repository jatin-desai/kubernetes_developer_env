# FROM store/oracle/serverjre:8
FROM openjdk:8-jdk-alpine
VOLUME /tmp
ARG APPD_AGENT
ARG INT_CERT_KEYSTORE

# TBD - appd placeholder
ADD ${APPD_AGENT} appd_agent.tar
RUN mv ./appd_agent.tar/AppServerAgent-4.2.15.18/ /home/appdynamics \
&& chown root:root /home/appdynamics

# TBD - internal certs placeholder
ADD ${INT_CERT_KEYSTORE} keystore.jks

# TBD - CONFIGURE SPLUNK Logging Driver placeholder
ENV LOG_DRIVER ${LOG_DRIVER}
